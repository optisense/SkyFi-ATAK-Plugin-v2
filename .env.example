# Environment Variables for SkyFi ATAK Plugin v2 Development

# Development Keystore Passwords (for local builds only)
DEBUG_KEYSTORE_PASSWORD=tnttnt
DEBUG_KEY_PASSWORD=tnttnt

# TAK Repository Credentials (optional - for accessing TAK artifacts)
TAKREPO_URL=https://artifacts.tak.gov/artifactory/maven
TAKREPO_USER=your_username
TAKREPO_PASSWORD=your_password

# Build Configuration
JAVA_HOME=/path/to/java-11
ANDROID_HOME=/path/to/android-sdk

# Plugin Development
TAKDEV_PLUGIN=.
TAK_STATIC_VERSION=1001

# Note: Copy this file to .env and update with your actual values
# Never commit .env files to version control!